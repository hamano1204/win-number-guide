<Window x:Class="WinNumberGuide.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinNumberGuide" Height="135" Width="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" WindowStartupLocation="Manual"
        UseLayoutRounding="True">
    
    <Window.Resources>
        <!-- Storyboards for fade in/out -->
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.15">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="MainContainer" Opacity="0" Background="#99000000" CornerRadius="12" Margin="10">
        <!-- Blur effect background -->
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Color="Black" Opacity="0.5" />
        </Border.Effect>
        
        <ItemsControl x:Name="AppsList" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="{Binding Margin}" Width="54">
                        <Grid>
                            <TextBlock Text="{Binding ShortcutNumber}" Foreground="#FFD700" FontSize="16" 
                                    FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Top"
                                    Margin="0,-6,-2,0"/>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Image Source="{Binding Icon}" Width="32" Height="32" Margin="0,2,0,4"/>
                                <TextBlock Text="{Binding Name}" Foreground="White" FontSize="9" 
                                    TextAlignment="Center" TextTrimming="CharacterEllipsis" MaxHeight="24"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</Window>
